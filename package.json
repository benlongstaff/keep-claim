{
	"name": "keep-claim",
	"version": "1.0.0",
	"description": "Script to check which keeps are eligible to have their rewards claimed but need to be notified",
	"main": "main.js",
	"dependencies": {
		"@keep-network/keep-core": "1.4.1",
		"@keep-network/keep-ecdsa": "1.3.1",
		"@keep-network/tbtc": "1.1.0",
		"ethers": "^5.0.0"
	},
	"devDependencies": {},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"license": "GPL-3.0-or-later"
}
